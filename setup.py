from setuptools import setup, find_packages

setup(
    name="groceries",
    version="1.0.0",
    description='Tempaltes for application',
    author='Simon Luo',

    packages=find_packages(),
    keywords=('groceries', 'groceriesclient', 'python27'),
)
